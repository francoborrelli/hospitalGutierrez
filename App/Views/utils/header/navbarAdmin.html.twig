{% import 'utils/header/navItem.html.twig' as navbar %}
        
{{ navbar.item('Pacientes', '/patients') }}

{% embed "utils/header/dropdown.html.twig" %}

    {% block elements %} 

        {{ navbar.dropdownItem('Usuarios', 'fa-user', '/admin/users', 'dropdown_users' ) }}

        <div class="dropdown-divider"></div>

        {{ navbar.dropdownItem('Roles', '#', 'dropdown_roles') }}

        <div class="dropdown-divider"></div>

        {{ navbar.dropdownItem('Permisos', '#', 'dropdown_permission') }}

        <div class="dropdown-divider"></div>

        {{ navbar.dropdownItem('Configuracion', '#', 'dropdown_config') }}
                
        <div class="dropdown-divider"></div>

        {% endblock %}

    {% endembed %}
