{% import 'utils/header/navItem.html.twig' as navbar %}
        

{% embed "utils/header/dropdown.html.twig" %}

    {% block elements %} 

        {% if currentUser.hasPermission("paciente_index") %}
            {{ navbar.dropdownItem("Pacientes", "fa-user", "/patients") }}
            <div class="dropdown-divider"></div>
        {% endif %}

        {% if currentUser.hasPermission("usuario_index") %}
            {{ navbar.dropdownItem("Usuarios", "fa-user", "/admin/users", "dropdown_users" ) }}
            <div class="dropdown-divider"></div>
        {% endif %}

        {% if currentUser.hasPermission("config_index") %}
            {{ navbar.dropdownItem("Configuracion", "fa-user", "/", "dropdown_config") }}
            <div class="dropdown-divider"></div>
        {% endif %}
 
        {{ navbar.dropdownItem("Roles", "fa-user", "/", "dropdown_roles") }}

        <div class="dropdown-divider"></div>

        {{ navbar.dropdownItem("Roles", "fa-user", "/", "dropdown_roles") }}
                
        <div class="dropdown-divider"></div>

        {% endblock %}

    {% endembed %}
