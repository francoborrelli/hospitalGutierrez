<div class="modal fade" id="deleteRecord" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <form method="post" action="/patient/{{patient.id}}/record/{{clinicalRecord.controlNumber}}/remove" id="deleteRecordForm">
                <div class="modal-header">
                    <h4 class="modal-title">Eliminar Control</h4>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Esta a punto de eliminar el control del día <span id="modalDate">{{clinicalRecord.controlDate|date('d/m/Y')}}</span>, ¿Está seguro que desea continuar?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-secondary">Eliminar</button>
                </div>
            </form>
        </div>
    </div>
</div>